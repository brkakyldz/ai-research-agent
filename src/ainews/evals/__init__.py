"""Evaluation: numbers about the output, regenerated by a command.

A fifth layer next to the four in `docs/HOW-IT-WORKS.md` §2, with one rule: it
reads the database and calls the pipeline's node functions, and nothing in the
pipeline or the web layer ever imports it. The product does not know it is
being measured.

`checks.py` is pure - rows in, numbers out - so the recorded fixture under
`tests/fixtures/runs/` and the live database feed the same code. Everything that
spends money (`judge`, `rank-stability`) is a sibling command, `ainews eval …`,
and never a test; `pytest` stays offline (PLAN-EVALS, ADR 0019).
"""
