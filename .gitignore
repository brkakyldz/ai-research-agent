# Secrets. The committed template is env.example.template.
.env
.env.*

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Runtime data (SQLite DB, WAL files, checkpoints, backups)
data/

# Editor/agent scratch
.claude/
.waltest*/
scratch/

# Agent working instructions and the research-store marker: local, not published.
CLAUDE.md
reports/.reports-architecture

# How the project is worked on, rather than what it does: the decision records,
# the milestone and evaluation plans, the design-round mockups and the research
# store. All of it stays on disk; none of it is published.
PLAN.md
DESIGN.md
docs/decisions/
docs/PLAN-EVALS.md
docs/PLAN-UI-INSTRUMENTS.md
docs/PLAN-V2.md
docs/AUDIT-*.md
docs/*.excalidraw
mockups/
reports/

# A second env template with the same keys and no placeholders. Nothing points
# at it; the README names env.example.template.
env.local.template
